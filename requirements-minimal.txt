# Minimal requirements for Render deployment
# Only essential packages to get the API running

# Core FastAPI
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP Client (essential)
httpx==0.25.2

# Basic data handling
python-dotenv==1.0.0

# If database is needed
sqlalchemy==2.0.23