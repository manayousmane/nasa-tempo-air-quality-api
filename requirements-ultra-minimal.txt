# Minimal requirements for Render deployment
# Only the absolute essentials

# Build tools first
setuptools>=68.0.0
wheel>=0.40.0

# Core web framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# HTTP client
httpx==0.25.2

# Environment
python-dotenv==1.0.0