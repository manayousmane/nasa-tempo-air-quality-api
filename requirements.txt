# NASA TEMPO Air Quality API - Minimal Production Dependencies
# Ultra-lightweight for fast Render deployment

# Core FastAPI (pre-compiled wheels available)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP client (lightweight)
httpx==0.25.2

# Essential utilities
python-dotenv==1.0.0